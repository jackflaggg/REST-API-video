export type  UserAPIModel = {
    /**
     * id: айди существующего пользователя
     * userName: логин существующего пользователя
     */
    id: number,
    userName: string
}

// что нам возвращается (клиенту )